import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

export const useCardStore = defineStore('card', () => {
  const cardList = ref([]);
  

// удаление карты 

// создание карты 

// редактирование 

// цвета


})

